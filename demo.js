/* eslint-disable */
'use-strict';

/**
 * @param {number[]} nums
 * @return {number}
 */
var findMaxLength = function (nums) {
  if (nums.length === 1) {
    if (nums[0] === 0) {
      const zeros = 1;
      const ones = 0;
    } else {
      const zeros = 0;
      const ones = 1;
    }
  }
};
